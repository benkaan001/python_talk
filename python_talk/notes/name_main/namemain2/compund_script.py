import compound_exercise  # overhead press

"""

This compound_exercise was actually not meant to be imported as a module.
It was meant to be run a script to access the function(s) inside.
However, when we import it, the function(s) inside the compound_exercise.py
automatically get invoked, creating the unwanted side effect.

"""
